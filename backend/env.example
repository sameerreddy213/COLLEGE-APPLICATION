# MongoDB Configuration
MONGODB_URI=mongodb+srv://IIITMDATABASEUSER:ochDOnjnml4VhuOD@iiitmanipur.itywtk3.mongodb.net/?retryWrites=true&w=majority&appName=iiitmanipur
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/manipur-digital-campus

# Redis Configuration (for caching)
REDIS_URL=redis://localhost:6379
# For production: redis://username:password@redis-host:6379

# JWT Configuration
JWT_SECRET=2a4c426ec22079f660a63471e803b482b7807f1ec5391252820c1ae2a72545cb903e6d8a8604622a8bb55bc7f04bf6fb42a38a582a0503d8c44bc08bb52615b5
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
# Multiple origins are now supported: http://localhost:5173, http://localhost:8080, http://localhost:8081, http://localhost:3000
# The backend will automatically allow these origins

# Rate Limiting (Optimized for 10,000 users)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=500

# Performance Settings (Optimized for 10,000 users)
# MongoDB connection pool settings
MONGODB_MAX_POOL_SIZE=100
MONGODB_MIN_POOL_SIZE=20

# Cache settings
CACHE_TTL=900

# Redis Cluster Settings (for high availability)
REDIS_CLUSTER_MODE=false
REDIS_MAX_RETRIES=3
REDIS_RETRY_DELAY=1000 